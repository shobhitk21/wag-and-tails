# The API the app talks to.
#
# Simulator or web:  http://localhost:4000
# Real device:       http://<your computer's LAN IP>:4000
#                    (localhost on a phone means the phone itself)
#
# Expo only exposes variables prefixed EXPO_PUBLIC_ to app code.
EXPO_PUBLIC_API_URL=http://localhost:4000
